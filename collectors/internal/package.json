{
  "name": "@dlr/internal-collector",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "uv --cache-dir run-data/uv-cache run --project python python -m unittest discover -s python/tests -v"
  },
  "devDependencies": { "tsx": "^4.20.5" }
}
