{
  "name": "@dlr/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@dlr/database": "workspace:*",
    "@fastify/cors": "^11.1.0",
    "fastify": "^5.5.0"
  },
  "devDependencies": {
    "tsx": "^4.20.5"
  }
}
